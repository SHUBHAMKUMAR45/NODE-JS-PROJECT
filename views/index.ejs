<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PROJECT</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 w-full rounded-md outline-none bg-zinc-800"
            type="text"
            placeholder="TITLE GOES HERE ....."
            name="title"
          />
          <textarea
            class="block px-5 py-2 w-full rounded-md outline-none resize-none mt-2 bg-zinc-800"
            placeholder="WRITE YOUR TASK DETAILS HERE ....."
            name="details"
          ></textarea>
          <input
            class="mt-4 bg-blue-400 rounded-lg outline-none px-5 py-2"
            type="submit"
            value="CREATE TASK "
          />
        </form>
      </div>
      <div class="task flex gap-4 p-10 flex-wrap">
        <% if(files.length>0 ){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
          <p class="text-white text-2xl tracking-tighter"><%= val %></p>
          <div class="flex w-full justify-between items-center m-2">
            <a class="text-blue-600 inline-block" href="/files/<%= val %>"
              >READ MORES
            </a>
            <a class="text-white" href="/edit/<%= val %>"> edit </a>
          </div>
        </div>
        <% }) %> <% } else{ %>
        <h3 class="text-white">NO TASK YET .....</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
